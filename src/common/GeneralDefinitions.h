#pragma once

/*! \brief Degree-to-radian conversion factor (π / 180). */
extern const double kDegToRad;

/*! \brief One radian expressed in degrees (180 / π). */
extern const double kOneRad;

/*! \brief Shared empty C string (avoid repeated string-literal allocations). */
extern const char *EMPTY_STRING;
